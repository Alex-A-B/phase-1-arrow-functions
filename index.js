// function expression called divide which divides 2000 by 100

const divide = function() {
    return 2000 / 100
}

// arrow function called square, takes one param and mutiplies by itself

const square = (parameter1) => parameter1 * parameter1;

// arrow function called add, takes two param and ads them together

const add = (param1, param2) => param1 + param2;